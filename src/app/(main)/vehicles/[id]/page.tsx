export default function VehicleDetailPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  return (
    <div>
      <h1>Detail Kendaraan</h1>
      {/* TODO: Vehicle detail with images, reviews, booking button */}
    </div>
  );
}
